export type ClassItem = {
    id: string;
    name: string;
    teacher: string;
    time: string[];
    rate: number;
    location: string;
    // ... other properties
  };

export type UserItem = {
    id: string;
    name: string;
    avatar: string;
}